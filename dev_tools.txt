calculate_top_overlapping_ratio(model_paths, percent)

poison_model(model, dev_device, noise_variance=1)

debug at the server side before aggr_model generated

Visualization tool

l1_prune(model=model,
        amount=amount,
        name='weight',
        verbose=self.args.prune_verbose)

get_pruned_amount_by_mask(model)

get_pruned_amount_by_weights(model)